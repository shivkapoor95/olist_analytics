version: 2

models:
  - name: stg_olist_orders
    description: "Staged orders table with cleaned and recast column types from the Olist source."
    columns:
      - name: order_id
        description: "Unique identifier for each order."
        tests:
          - unique
          - not_null
      - name: customer_id
        description: "References the customer who placed the order."
        tests:
          - not_null
          - relationships:
              to: ref('stg_olist_customers')
              field: customer_id
      - name: order_status
        description: "Current status of the order."
        tests:
          - accepted_values:
              values: ['delivered', 'shipped', 'canceled', 'unavailable', 'invoiced', 'processing', 'created', 'approved']
      - name: order_purchase_timestamp
        description: "Timestamp when the order was placed."
      - name: order_approved_at
        description: "Timestamp when payment was approved."
      - name: order_delivered_carrier_date
        description: "Timestamp when order was handed to the carrier."
      - name: order_delivered_customer_date
        description: "Timestamp when order was delivered to the customer."
      - name: order_estimated_delivery_date
        description: "Estimated delivery date given to the customer."

  - name: stg_olist_customers
    description: "Staged customers table with customer location and identity information."
    columns:
      - name: customer_unique_id
        description: "True unique identifier for a customer across all orders."
        tests:
          - not_null
      - name: customer_id
        description: "Unique identifier per order — same customer gets a new id for each order."
        tests:
          - not_null
      - name: customer_zip_code_prefix
        description: "Zip code prefix of the customer's location."
      - name: customer_city
        description: "City of the customer."
      - name: customer_state
        description: "State of the customer."

  - name: stg_olist_order_items
    description: "Staged order items table with product, seller, price and freight information."
    columns:
      - name: order_id
        description: "References the order this item belongs to."
        tests:
          - not_null
          - relationships:
              to: ref('stg_olist_orders')
              field: order_id
      - name: order_item_id
        description: "Sequential number identifying the item within an order."
      - name: product_id
        description: "References the product that was purchased."
        tests:
          - not_null
          - relationships:
              to: ref('stg_olist_products')
              field: product_id
      - name: seller_id
        description: "References the seller who sold the item."
        tests:
          - not_null
          - relationships:
              to: ref('stg_olist_sellers')
              field: seller_id
      - name: shipping_limit_date
        description: "Deadline for the seller to hand the item to the carrier."
      - name: price
        description: "Price of the item in Brazilian Real."
      - name: freight_value
        description: "Freight cost for the item in Brazilian Real."

  - name: stg_olist_order_payments
    description: "Staged payments table with payment method and value per order."
    columns:
      - name: order_id
        description: "References the order this payment belongs to."
        tests:
          - not_null
          - relationships:
              to: ref('stg_olist_orders')
              field: order_id
      - name: payment_sequential
        description: "Sequence number for payments — an order can have multiple payment methods."
      - name: payment_type
        description: "Payment method used e.g. credit_card, boleto, voucher."
      - name: payment_installments
        description: "Number of installments chosen by the customer."
      - name: payment_value
        description: "Transaction value in Brazilian Real."

  - name: stg_olist_order_reviews
    description: "Staged reviews table with customer satisfaction scores and comments."
    columns:
      - name: review_id
        description: "Unique identifier for each review."
        tests:
          - not_null
      - name: order_id
        description: "References the order being reviewed."
        tests:
          - not_null
          - relationships:
              to: ref('stg_olist_orders')
              field: order_id
      - name: review_score
        description: "Customer satisfaction score from 1 (worst) to 5 (best)."
      - name: review_comment_title
        description: "Title of the review left by the customer."
      - name: review_comment_message
        description: "Full review message left by the customer."
      - name: review_creation_date
        description: "Date the review survey was sent to the customer."
      - name: review_answer_timestamp
        description: "Timestamp when the customer submitted the review."

  - name: stg_olist_products
    description: "Staged products table with product details and category information."
    columns:
      - name: product_id
        description: "Unique identifier for each product."
        tests:
          - unique
          - not_null
      - name: product_category_name
        description: "Category name of the product in Portuguese."
      - name: product_weight_g
        description: "Product weight in grams."
      - name: product_length_cm
        description: "Product length in centimeters."
      - name: product_height_cm
        description: "Product height in centimeters."
      - name: product_width_cm
        description: "Product width in centimeters."

  - name: stg_olist_sellers
    description: "Staged sellers table with seller location information."
    columns:
      - name: seller_id
        description: "Unique identifier for each seller."
        tests:
          - unique
          - not_null
      - name: seller_zip_code_prefix
        description: "Zip code prefix of the seller's location."
      - name: seller_city
        description: "City where the seller is located."
      - name: seller_state
        description: "State where the seller is located."

  - name: stg_olist_product_category_name_translation
    description: "Staged product category translation table mapping Portuguese to English names."
    columns:
      - name: product_category_name
        description: "Product category name in Portuguese."
        tests:
          - unique
          - not_null
      - name: product_category_name_english
        description: "Product category name translated to English."